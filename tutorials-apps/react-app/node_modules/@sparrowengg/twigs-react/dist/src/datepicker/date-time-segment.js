import { jsx as _jsx } from "react/jsx-runtime";
import { useRef } from "react";
import { useDateSegment } from "react-aria";
import { Box } from "../box";
import { Text } from "../text";
export function DateTimeSegment({ segment, state }) {
    let ref = useRef(null);
    let { segmentProps } = useDateSegment(segment, state, ref);
    return (_jsx(Box, { ...segmentProps, ref: ref, style: {
            ...segmentProps.style
        }, css: {
            outline: 'none',
            textAlign: 'center',
            color: '$black900',
            borderRadius: '$xs',
            minWidth: segment.maxValue != null ? String(segment.maxValue).length + "ch" : 'auto',
            '&:focus': {
                background: '$black100'
            }
        }, children: _jsx(Text, { size: 'md', weight: 'bold', css: {
                color: '$neutral800'
            }, children: segment.text }) }));
}
