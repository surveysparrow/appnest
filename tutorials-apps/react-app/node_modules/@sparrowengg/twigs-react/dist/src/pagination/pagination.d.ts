import { ComponentProps, FunctionComponent, SyntheticEvent } from 'react';
import { Button } from '../button';
declare type ButtonSizeType = Pick<ComponentProps<typeof Button>, 'size'>;
export declare type PaginationProps = {
    total: number;
    itemsPerPage: number;
    activePage: number;
    siblingCount?: number;
    onChange?: (event: SyntheticEvent, page: number) => void;
} & ButtonSizeType;
/**
 * @param total total items inside pagination
 * @param itemsPerPage number of items in each age
 * @param activePage currently focused page
 * @param onChange returns click event and active page number
 */
export declare const Pagination: FunctionComponent<PaginationProps>;
export {};
