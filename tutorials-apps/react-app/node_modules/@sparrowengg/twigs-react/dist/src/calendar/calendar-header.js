import { jsx as _jsx } from "react/jsx-runtime";
import { styled } from '../../stitches.config';
import { IconButton } from '../button';
import { Flex } from '../flex';
export const CalendarNavigationButton = ({ icon, onPress, isDisabled, ...rest }) => {
    return (_jsx(IconButton, { color: "default", ...rest, onClick: onPress, icon: icon, size: "lg", disabled: isDisabled }));
};
export const CalendarHeader = styled(Flex, {
    alignItems: 'center',
    justifyContent: 'space-between',
    height: '64px'
});
export const CalendarMonth = styled('span', {
    color: '$neutral800',
    fontSize: '$md',
    fontWeight: '$7'
});
