import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useRef } from 'react';
import { createCalendar } from '@internationalized/date';
import { useCalendar, useLocale } from 'react-aria';
import { useCalendarState } from 'react-stately';
import { ChevronLeftIcon, ChevronRightIcon } from '@sparrowengg/twigs-react-icons';
import { Box } from '../box';
import { CalendarMonth, CalendarHeader, CalendarNavigationButton } from './calendar-header';
import { CalendarGrid } from './calendar-grid';
export const Calendar = (props) => {
    const { locale } = useLocale();
    const state = useCalendarState({
        ...props,
        locale,
        createCalendar
    });
    const ref = useRef(null);
    const { calendarProps, prevButtonProps, nextButtonProps, title } = useCalendar(props, state);
    return (_jsxs(Box, { ...calendarProps, ref: ref, children: [_jsxs(CalendarHeader, { children: [_jsx(CalendarNavigationButton, { ...prevButtonProps, icon: _jsx(ChevronLeftIcon, {}) }), _jsx(CalendarMonth, { children: title }), _jsx(CalendarNavigationButton, { ...nextButtonProps, icon: _jsx(ChevronRightIcon, {}) })] }), _jsx(CalendarGrid, { state: state })] }));
};
