import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { Box } from '../box';
const widthToColumns = (width, repeat) => `repeat(auto-${repeat}, minmax(${width}px, 1fr))`;
export const Grid = React.forwardRef(({ children, width, templateColumns, templateRows, templateAreas, gap = [3, 3], repeat = 'fit', css, ...rest }, ref) => {
    const [columnGap, rowGap] = gap;
    const gridTemplateColumns = width
        ? widthToColumns(width, repeat)
        : templateColumns;
    return (_jsx(Box, { ref: ref, "data-testid": "grid", css: {
            display: 'grid',
            rowGap,
            columnGap,
            gridTemplateColumns,
            gridTemplateRows: templateRows,
            gridTemplateAreas: templateAreas,
            ...css
        }, ...rest, children: children }));
});
