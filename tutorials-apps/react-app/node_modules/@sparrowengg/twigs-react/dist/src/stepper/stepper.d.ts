import React, { FunctionComponent } from 'react';
import { BoxProps } from '../box';
export declare type StepperProps = {
    activeStep: number;
    children: React.ReactElement[];
    onChange?: (step: number) => void;
    components?: {
        TriggerSeparator?: React.ComponentType;
        TriggerContainer?: React.ComponentType<{
            children: React.ReactNode;
        }>;
        TriggerButton?: React.ComponentType<{
            children: React.ReactNode;
            active?: boolean;
            completed?: boolean;
        }>;
    };
} & BoxProps;
declare type StepperItemProps = {
    children: React.ReactNode;
    label: string;
    allowClick?: boolean;
} & BoxProps;
declare const StepperItem: ({ children, label, allowClick, ...props }: StepperItemProps) => JSX.Element;
declare const Stepper: FunctionComponent<StepperProps>;
export { Stepper, StepperItem };
