import React from 'react';
import { theme as stichesTheme } from '../../stitches.config';
export declare const ThemeContext: React.Context<any>;
declare type ThemeProviderProps = {
    theme: typeof stichesTheme | {};
    children: React.ReactNode;
};
export declare const ThemeProvider: ({ theme, children }: ThemeProviderProps) => JSX.Element | null;
export {};
