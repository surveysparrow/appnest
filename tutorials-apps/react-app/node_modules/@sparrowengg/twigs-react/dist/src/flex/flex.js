import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { styled } from '../../stitches.config';
import { Box } from '../box';
const StyledBox = styled(Box, {});
export const Flex = React.forwardRef(({ children, flexDirection, justifyContent, justifyItems, alignContent, alignItems, gap, wrap, shrink, css, grow, flow, basis, ...rest }, ref) => {
    return (_jsx(StyledBox, { css: {
            display: 'flex',
            flexDirection,
            justifyContent,
            justifyItems,
            alignContent,
            alignItems,
            gap,
            flexShrink: shrink,
            flexWrap: wrap,
            flexGrow: grow,
            flexFlow: flow,
            flexBasis: basis,
            ...css
        }, "data-testid": "flex", ...rest, ref: ref, children: children }));
});
