import { jsx as _jsx } from "react/jsx-runtime";
import React, { isValidElement } from 'react';
import { Button } from './button';
export const IconButton = React.forwardRef(({ children, icon, 'aria-label': ariaLabel, ...rest }, ref) => {
    const element = icon || children;
    const validElement = isValidElement(element) ? element : null;
    return (_jsx(Button, { ref: ref, "aria-label": ariaLabel, icon: validElement, ...rest }));
});
